# TC-605: Security Audit
**Phase**: 6 | **Priority**: P0 | **Estimate**: 12h
**Assignee**: AI-Pipeline | **Depends On**: Phase 5 Gate
**Feature Flag**: N/A (security verification)

## 🎯 OBJECTIVE
Security audit แบบครบถ้วน: RLS verification, auth boundaries, secrets scanning, rate limits, XSS/CSRF protection — documented และ CI gates ready

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `e2e/security.spec.ts`
- [x] RLS: ทุก public.* table มี policy `auth.uid() = user_id` verified
- [x] Auth boundaries: JWT validation, session expiry, refresh token rotation
- [x] Secrets: ไม่มี secrets ใน codebase (git-secrets scan), .env ไม่อยู่ใน git
- [x] Rate limits: API endpoints มี rate limiting (Supabase + Cloudflare)
- [x] XSS: CSP headers, input sanitization, output encoding verified
- [x] CSRF: SameSite cookies, CSRF tokens สำหรับ state-changing ops
- [x] Security headers: HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy
- [x] Runs in `chromium-staging` project
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### RLS Verification (35 migrations)
```typescript
test('public.twins has RLS policy', async () => {
  const { data, error } = await supabase.from('twins').select('*');
  expect(error).toBeNull();
  // Verify policy exists via pg_policies
});

test('public.decisions user-scoped', async () => { ... });
test('public.conversations user-scoped', async () => { ... });
test('selfprint.* uses service_role via API only', async () => { ... });
```

### Auth Boundaries
```typescript
test('JWT expires correctly', async ({ page }) => { ... });
test('refresh token rotates on use', async ({ page }) => { ... });
test('concurrent sessions limited', async ({ page }) => { ... });
test('logout revokes all sessions', async ({ page }) => { ... });
```

### Secrets Scanning
```typescript
test('no secrets in tracked files', async () => {
  const results = await runGitSecretsScan();
  expect(results.violations).toHaveLength(0);
});

test('.env files in .gitignore', async () => { ... });
test('no hardcoded API keys in source', async () => { ... });
```

### Rate Limits
```typescript
test('API rate limit enforced (100 req/min)', async ({ page }) => { ... });
test('auth rate limit (5 attempts/15min)', async ({ page }) => { ... });
test('file upload rate limit', async ({ page }) => { ... });
```

### XSS/CSRF
```typescript
test('CSP header present and strict', async ({ page }) => {
  const headers = await page.goto('/th');
  expect(headers.headers()['content-security-policy']).toContain("default-src 'self'");
});

test('input sanitization prevents XSS', async ({ page }) => { ... });
test('CSRF token required for mutations', async ({ page }) => { ... });
test('SameSite=Lax on auth cookies', async ({ page }) => { ... });
```

### Security Headers
```typescript
test('HSTS header present', async ({ page }) => { ... });
test('X-Frame-Options: DENY', async ({ page }) => { ... });
test('X-Content-Type-Options: nosniff', async ({ page }) => { ... });
test('Referrer-Policy: strict-origin-when-cross-origin', async ({ page }) => { ... });
```

## 🧪 TEST SPEC
```typescript
// Security test categories
test.describe('RLS policies', () => { ... });
test.describe('Auth boundaries', () => { ... });
test.describe('Secrets hygiene', () => { ... });
test.describe('Rate limiting', () => { ... });
test.describe('XSS protection', () => { ... });
test.describe('CSRF protection', () => { ... });
test.describe('Security headers', () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- E2E test: e2e/security.spec.ts
- RLS verified on 35 migrations
- Secrets scan integrated in CI
- Security headers verified