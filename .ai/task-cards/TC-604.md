# TC-604: Performance Baselines
**Phase**: 6 | **Priority**: P0 | **Estimate**: 12h
**Assignee**: AI-Frontend | **Depends On**: Phase 5 Gate
**Feature Flag**: N/A (test infrastructure)

## 🎯 OBJECTIVE
กำหนด performance budgets และสร้าง CI gates: LCP, TBT, CLS, FID, bundle size, resource loading — integrate กับ Lighthouse CI

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `e2e/performance.spec.ts`
- [x] LCP budget: < 2.5s (mobile), < 2.0s (desktop)
- [x] TBT budget: < 150ms (mobile), < 100ms (desktop)
- [x] CLS budget: < 0.1 (all)
- [x] FID/INP budget: < 100ms (all)
- [x] Bundle size: main chunk < 200KB gzip, total JS < 500KB gzip
- [x] Resource loading: critical CSS inlined, fonts preloaded, images optimized
- [x] Lighthouse CI integration: thresholds ใน `lighthouserc.json`
- [x] Runs in `chromium` + `mobile-chrome` projects
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### Performance Budgets (lighthouserc.json)
```json
{
  "ci": {
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.9 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500, "aggregationMethod": "median" }],
        "total-blocking-time": ["error", { "maxNumericValue": 150, "aggregationMethod": "median" }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1, "aggregationMethod": "median" }],
        "interaction-to-next-paint": ["error", { "maxNumericValue": 100, "aggregationMethod": "median" }]
      }
    }
  }
}
```

### E2E Performance Tests
```typescript
test('LCP < 2.5s on landing page (mobile)', async ({ page }) => {
  await page.goto('/th', { waitUntil: 'networkidle' });
  const lcp = await page.evaluate(() => new Promise(resolve => {
    new PerformanceObserver(list => {
      const entries = list.getEntriesByType('largest-contentful-paint');
      resolve(entries[entries.length - 1].startTime);
    }).observe({ type: 'largest-contentful-paint', buffered: true });
  }));
  expect(lcp).toBeLessThan(2500);
});

test('TBT < 150ms on dashboard', async ({ page }) => { ... });
test('CLS < 0.1 on all pages', async ({ page }) => { ... });
test('bundle size within budget', async () => {
  const stats = await getBuildStats();
  expect(stats.mainChunkGzip).toBeLessThan(200 * 1024);
  expect(stats.totalJSGzip).toBeLessThan(500 * 1024);
});
```

### Resource Loading Checks
```typescript
test('critical CSS inlined', async ({ page }) => { ... });
test('fonts preloaded with preconnect', async ({ page }) => { ... });
test('images have width/height + loading=lazy', async ({ page }) => { ... });
test('no render-blocking resources', async ({ page }) => { ... });
```

## 🧪 TEST SPEC
```typescript
// Performance test categories
test.describe('Core Web Vitals', () => { ... });
test.describe('Bundle size budgets', () => { ... });
test.describe('Resource loading optimization', () => { ... });
test.describe('Runtime performance', () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- E2E test: e2e/performance.spec.ts
- Lighthouse CI config: lighthouserc.json
- Runs in chromium + mobile-chrome
- Budgets documented for CI gates