# TC-602: Negative/Edge Case Coverage
**Phase**: 6 | **Priority**: P0 | **Estimate**: 16h
**Assignee**: AI-Frontend | **Depends On**: Phase 5 Gate
**Feature Flag**: N/A (test infrastructure)

## 🎯 OBJECTIVE
สร้าง E2E tests ครอบคลุม edge cases และ error scenarios: Auth failures, empty states, network errors, invalid inputs, boundary conditions

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `e2e/negative-cases.spec.ts`
- [x] Auth: expired token, revoked session, invalid credentials, OAuth error
- [x] Empty states: no twin, no memories, no decisions, no worlds
- [x] Network: offline, timeout, 5xx errors, rate limiting
- [x] Inputs: invalid DOB, malformed decisions, XSS attempts, SQL injection
- [x] Boundaries: max file upload, max decision length, max memory items
- [x] Error boundaries: React error boundaries catch และ display gracefully
- [x] Runs in `chromium-staging` project
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### Auth Edge Cases
```typescript
test('expired token redirects to login', async ({ page }) => {
  await page.goto('/th/dashboard');
  await page.evaluate(() => localStorage.setItem('sb-token', 'expired'));
  await page.reload();
  await expect(page).toHaveURL(/\/login/);
});

test('revoked session shows re-auth prompt', async ({ page }) => { ... });
test('OAuth callback error handled gracefully', async ({ page }) => { ... });
```

### Empty States
```typescript
test('no twin shows onboarding CTA', async ({ page }) => { ... });
test('no memories shows empty state illustration', async ({ page }) => { ... });
test('no decisions shows create first decision CTA', async ({ page }) => { ... });
```

### Network Errors
```typescript
test('offline mode shows cached data + banner', async ({ page }) => { ... });
test('API timeout shows retry option', async ({ page }) => { ... });
test('5xx error shows friendly error page', async ({ page }) => { ... });
test('rate limit shows wait timer', async ({ page }) => { ... });
```

### Input Validation
```typescript
test('invalid DOB format shows inline error', async ({ page }) => { ... });
test('XSS in decision input sanitized', async ({ page }) => { ... });
test('SQL injection attempt blocked', async ({ page }) => { ... });
test('max file size (10MB) enforced', async ({ page }) => { ... });
```

### Error Boundaries
```typescript
test('React error boundary catches component crash', async ({ page }) => { ... });
test('error boundary shows fallback UI + report button', async ({ page }) => { ... });
```

## 🧪 TEST SPEC
```typescript
// Negative case categories
test.describe('Auth failures', () => { ... });
test.describe('Empty states', () => { ... });
test.describe('Network errors', () => { ... });
test.describe('Input validation', () => { ... });
test.describe('Boundary conditions', () => { ... });
test.describe('Error boundaries', () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- E2E test: e2e/negative-cases.spec.ts
- 50+ test cases covering all error scenarios
- Runs in CI chromium-staging