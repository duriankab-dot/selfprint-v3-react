# TC-501: LivingTwin Dashboard
**Phase**: 5 | **Priority**: P0 | **Estimate**: 16h
**Assignee**: AI-Frontend | **Depends On**: Phase 4 Gate
**Feature Flag**: LIVING_TWIN

## 🎯 OBJECTIVE
สร้าง LivingTwin Dashboard component ที่รวม MemoryPanel, EvolutionTimeline, InsightCards เข้าไว้ด้วยกัน — แสดงการเติบโตของ AI Twin แบบ real-time

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `src/components/livingTwin/MemoryPanel.tsx` — แสดง memory insights, search, filter
- [x] สร้าง `src/components/livingTwin/EvolutionTimeline.tsx` — timeline แสดง version progression
- [x] สร้าง `src/components/livingTwin/InsightCards.tsx` — actionable insights จาก memory + decisions
- [x] สร้าง `src/components/dashboard/LivingTwin.tsx` — container component รวมทั้ง 3
- [x] Integration กับ TwinContext + TwinStore
- [x] Unit tests: ผ่าน
- [x] E2E tests: `e2e/living-twin.spec.ts` มี spec พร้อม
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### MemoryPanel
- ใช้ `useMemoryInsights` hook
- Search + filter + relevance scoring
- Context preview on click

### EvolutionTimeline
- ใช้ `useEvolution` hook
- แสดง version transitions (v1→v2→v3)
- Trigger attribution (what caused evolution)

### InsightCards
- Actionable insights จาก memory patterns + decision outcomes
- Priority scoring

### LivingTwin (Dashboard)
- Responsive grid layout
- Real-time updates via TwinContext subscription

## 🧪 TEST SPEC
```typescript
// LivingTwin.test.tsx
test('renders MemoryPanel + EvolutionTimeline + InsightCards', () => { ... });
test('updates in real-time when TwinStore changes', () => { ... });
test('MemoryPanel search filters correctly', () => { ... });
test('EvolutionTimeline shows version diff on click', () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- New components: MemoryPanel, EvolutionTimeline, InsightCards, LivingTwin
- E2E test: e2e/living-twin.spec.ts
- Hooks: useMemoryInsights, useEvolution