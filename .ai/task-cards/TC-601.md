# TC-601: Cross-domain Integration Tests
**Phase**: 6 | **Priority**: P0 | **Estimate**: 16h
**Assignee**: AI-Frontend | **Depends On**: Phase 5 Gate
**Feature Flag**: N/A (test infrastructure)

## 🎯 OBJECTIVE
สร้าง E2E tests ครอบคลุม user journey ทั้งหมด: Birth → Memory → Evolution → Decision → Worlds — verify cross-domain integration

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `e2e/cross-domain.spec.ts`
- [x] Test 1: Full birth flow → twin created → memory captured
- [x] Test 2: Decision created → twin learns → evolution triggered
- [x] Test 3: World interaction → memory stored → intelligence updated
- [x] Test 4: Session persistence → restore → all domains accessible
- [x] Test 5: Multi-tab sync → cross-tab state consistent
- [x] ใช้ fixtures: user-twin-alive, user-awakening
- [x] Runs in `chromium-staging` project
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### Test Structure
```typescript
test.describe('Cross-domain: Birth → Memory → Evolution → Decision → Worlds', () => {
  test('complete lifecycle: birth → first memory → first decision → evolution → world entry', async ({ page }) => {
    // 1. Birth flow
    await page.goto('/th/core-awakening');
    await completeBirthFlow(page);
    
    // 2. First memory (chat interaction)
    await page.goto('/th/chat/twin');
    await sendMessage(page, 'Test message for memory');
    await expectMemoryCaptured(page);
    
    // 3. First decision
    await page.goto('/th/decision/new');
    await createDecision(page, { context: 'Test decision' });
    await expectDecisionPersisted(page);
    
    // 4. Evolution triggered
    await expectEvolutionTriggered(page);
    await verifyEvolutionDiff(page);
    
    // 5. World entry
    await page.goto('/th/worlds');
    await enterWorld(page, 'career');
    await expectWorldIntelligence(page);
  });
});
```

### Fixtures
- `user-twin-alive`: TWIN_ALIVE lifecycle, has twin
- `user-awakening`: AWAKENING lifecycle, no twin

### Assertions
- Memory appears in MemoryInsightsPage
- EvolutionTimeline shows new version
- IntelligenceHub aggregates all domains
- Cross-tab sync verified

## 🧪 TEST SPEC
```typescript
// Cross-domain integration scenarios
test('birth → memory → decision → evolution → world', async () => { ... });
test('session restore preserves all domain state', async () => { ... });
test('multi-tab sync keeps intelligence consistent', async () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- E2E test: e2e/cross-domain.spec.ts
- Uses existing fixtures
- Runs in CI chromium-staging