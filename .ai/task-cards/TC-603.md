# TC-603: Mobile E2E Suite
**Phase**: 6 | **Priority**: P0 | **Estimate**: 16h
**Assignee**: AI-Frontend | **Depends On**: Phase 5 Gate
**Feature Flag**: N/A (test infrastructure)

## 🎯 OBJECTIVE
สร้าง Mobile E2E test suite: touch targets, scroll behavior, PWA offline, viewport handling, safe area — runs ใน Mobile Chrome + Mobile Safari projects

## 📋 DEFINITION OF DONE (ALL REQUIRED)
- [x] สร้าง `e2e/mobile.spec.ts`
- [x] Touch targets: minimum 48x48px, spacing 8px
- [x] Scroll: momentum, overscroll, pull-to-refresh blocked
- [x] PWA: offline banner, install prompt, service worker cache
- [x] Viewport: 375px, 390px, 414px widths, landscape/orientation
- [x] Safe area: notch/Dynamic Island, home indicator, bottom bar
- [x] Runs in `mobile-chrome` + `mobile-safari` projects
- [x] **Docs updated**: อัพเดท MASTER_PLAN.md task status
- [x] **All tests pass**: `npm run typecheck && npm run lint && npm test && npm run build`

## 🔧 IMPLEMENTATION NOTES
### Touch Targets
```typescript
test('all interactive elements meet 48x48 minimum', async ({ page }) => {
  const elements = await page.locator('button, a, input, select, [role="button"]').all();
  for (const el of elements) {
    const box = await el.boundingBox();
    expect(box?.width).toBeGreaterThanOrEqual(48);
    expect(box?.height).toBeGreaterThanOrEqual(48);
  }
});

test('touch target spacing >= 8px', async ({ page }) => { ... });
```

### Scroll Behavior
```typescript
test('momentum scroll works on long pages', async ({ page }) => { ... });
test('overscroll bounce disabled on main content', async ({ page }) => { ... });
test('pull-to-refresh blocked on non-list pages', async ({ page }) => { ... });
```

### PWA Offline
```typescript
test('offline banner appears when disconnected', async ({ page }) => {
  await page.context().setOffline(true);
  await page.reload();
  await expect(page.locator('[data-testid="offline-banner"]')).toBeVisible();
});

test('service worker caches critical assets', async ({ page }) => { ... });
test('install prompt appears on eligible visits', async ({ page }) => { ... });
```

### Viewport Testing
```typescript
const viewports = [
  { width: 375, height: 667, name: 'iPhone SE' },
  { width: 390, height: 844, name: 'iPhone 12/13/14' },
  { width: 414, height: 896, name: 'iPhone 11/Pro Max' },
];

for (const vp of viewports) {
  test(`layout works at ${vp.name} (${vp.width}x${vp.height})`, async ({ page }) => {
    await page.setViewportSize({ width: vp.width, height: vp.height });
    await page.goto('/th');
    await expectNoHorizontalScroll(page);
  });
  
  test(`landscape works at ${vp.name}`, async ({ page }) => { ... });
}
```

### Safe Area
```typescript
test('content respects safe-area-inset-top (notch)', async ({ page }) => { ... });
test('content respects safe-area-inset-bottom (home indicator)', async ({ page }) => { ... });
test('bottom nav above safe-area-inset-bottom', async ({ page }) => { ... });
test('modal/drawer respects safe-area-inset-bottom', async ({ page }) => { ... });
```

## 🧪 TEST SPEC
```typescript
// Mobile test categories
test.describe('Touch targets', () => { ... });
test.describe('Scroll behavior', () => { ... });
test.describe('PWA offline', () => { ... });
test.describe('Viewport & orientation', () => { ... });
test.describe('Safe area insets', () => { ... });
```

## 📦 HANDOFF ARTIFACTS
- Updated MASTER_PLAN.md
- E2E test: e2e/mobile.spec.ts
- Runs in mobile-chrome + mobile-safari projects
- 30+ test cases across viewports