# เอกสารตรวจสอบที่ 7: แผนการอพยพข้อมูล

**การเปลี่ยนแปลงโครงการฐานข้อมูล + การจัดการข้อมูลประวัติศาสตร์**

---

## ตารางฐานข้อมูล Supabase ใหม่

### ตารางที่ 1: analysis_history
```sql
CREATE TABLE analysis_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  analysis_type TEXT NOT NULL CHECK (analysis_type IN ('initial', 'refined', 'journal_pattern', 'decision')),
  
  data JSONB NOT NULL,
  sources TEXT[] NOT NULL,
  confidence FLOAT DEFAULT 0.85,
  
  created_at TIMESTAMP DEFAULT now(),
  ttl_days INT DEFAULT 365
);

CREATE INDEX analysis_history_user_created ON analysis_history(user_id, created_at DESC);
```

### ตารางที่ 2: pattern_insights
```sql
CREATE TABLE pattern_insights (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  
  pattern TEXT NOT NULL,
  pattern_category TEXT,
  
  first_detected TIMESTAMP,
  last_seen TIMESTAMP,
  frequency INT DEFAULT 1,
  confidence FLOAT DEFAULT 0.75,
  
  created_at TIMESTAMP DEFAULT now()
);

CREATE INDEX pattern_insights_user ON pattern_insights(user_id, created_at DESC);
```

### ตารางที่ 3: session_logs
```sql
CREATE TABLE session_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  
  action TEXT NOT NULL,
  request_size INT,
  response_size INT,
  latency_ms INT,
  
  status TEXT CHECK (status IN ('success', 'fallback', 'error')),
  error_message TEXT,
  
  created_at TIMESTAMP DEFAULT now()
);

CREATE INDEX session_logs_user ON session_logs(user_id, created_at DESC);
```

---

## การเปลี่ยนแปลงโครงการที่มีอยู่

### auth.users (ขยายโปรไฟล์)
```json
{
  "profile_metadata": {
    "astrovera_user_id": "string",
    "preferred_modules": ["psychology", "numerology"],
    "timezone": "string"
  }
}
```

### ตารางผู้ใช้ที่มีอยู่
- เพิ่มฟิลด์: `last_analysis_at`
- เพิ่มฟิลด์: `analysis_count`

---

## ความปลอดภัยและสิทธิ์

### นโยบาย RLS
- `analysis_history`: ผู้ใช้อ่านของตนเอง
- `pattern_insights`: ผู้ใช้อ่านของตนเอง
- `session_logs`: อ่านผู้ใช้ + admin ทั้งหมด

### การเข้ารหัส
- สำคัญ Astrovera: Edge Function สภาพแวดล้อม เท่านั้น
- ข้อมูลผู้ใช้: ข้อมูลอ่าน Supabase ปกติ

---

**เอกสารเสร็จสิ้น** ✅
