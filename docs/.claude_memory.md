# 🧠 Claude Memory — SELFPRINT v3-react

**อ่านไฟล์นี้ก่อนเสมอเมื่อเริ่มแชทใหม่ในโปรเจกต์นี้**

---

## ⚠️ คำเตือนเรื่องเอกสารเก่า

`docs/` มีไฟล์ handoff/phase-summary เก่าเยอะมาก — **ห้ามเชื่อว่าไฟล์เหล่านั้นบอกสถานะปัจจุบัน**

**สถานะจริง ให้เช็คจากโค้ด/git เสมอ:**
```bash
git log --oneline -10
git status --short
cat src/App.tsx
npx tsc -b
```

---

## 👤 User Profile

**Name:** jb_DEV | **Email:** duriankab@gmail.com
**Style:** ภาษาไทยกันเองแต่สุภาพ, สั้นกระชับตรงประเด็น, ทำงานมีวินัยไม่ใช่แค่เขียนเร็ว
**Approach:** เข้าใจโจทย์จริงก่อน → ทำเท่าที่ขอ → แก้เฉพาะจุด → ทุกงาน verify ได้

---

## 📌 สถานะล่าสุด (อัปเดต 2026-08-11)

**Last commit:** `3957496` feat: §5.2 Dynamic วันนี้ Home
**Branch:** master — pushed + deployed ✅

**tsc -b:** EXIT 0 ✅
**npm run build:** รันได้ปกติบน Windows/Vercel (sandbox Linux ≠ platform)

---

## 🗺️ Navigation Architecture (§5.1) — COMPLETE ✅

5 tabs ตาม Master Directive:
| Tab | Label | Route | หน้า |
|-----|-------|-------|------|
| 1 | วันนี้ | `/dashboard` | Dashboard + TodaySection |
| 2 | สำรวจ | `/explore` | ExplorePage |
| 3 | กิจกรรม | `/activities` | ActivitiesPage |
| 4 | AI ฝาแฝด | `/chat` | Chat/ChatPage |
| 5 | ฉัน | `/me` | MePage |

BottomNav: `src/components/layout/BottomNav.tsx` (5 equal tabs, var(--...) only)

---

## 📋 Routes ทั้งหมดใน App.tsx

```
/                   → LandingPage
/onboarding         → Onboarding (7-step)
/chat               → Chat → ChatPage
/dashboard          → Dashboard (วันนี้ + TodaySection + analytics)
/explore            → ExplorePage ✨ NEW
/activities         → ActivitiesPage ✨ NEW
/me                 → MePage ✨ NEW
/analysis           → AnalysisPage
/privacy            → PrivacyCenter (PDPA)
/share/:code        → Share
/brief              → DailyBriefPage (§25)
/badges             → BadgePage (§29-30)
/pricing            → PricingPage (§31)
/pricing/success    → PricingSuccessPage
/login              → LoginPage (§34 Passkey/OAuth/Magic Link)
/settings/passkeys  → PasskeySettings (§34)
/menu               → FeatureMenu
/components         → ComponentShowcase
```

### Orphan pages (ไฟล์มีแต่ยังไม่มี route)
- `src/pages/VoiceChatPage.tsx` — Voice chat (future)
- `src/pages/TwinProfilePage.tsx` — Twin profile (future)
- `src/pages/LifeHubsPage.tsx` — Life hubs (future)
- `src/pages/DecisionLoggerPage.tsx` — Decision logger (future)

**ถ้าต้องการใช้ต้องเพิ่ม Route ใน App.tsx + link ใน BottomNav/MePage**

---

## 🧩 Supabase Edge Functions (12 ฟังก์ชัน — ครบแล้ว)

```
supabase/functions/
├── auth-registration-options   → WebAuthn §34
├── auth-register-passkey        → WebAuthn §34
├── auth-authentication-options  → WebAuthn §34
├── auth-verify-passkey          → WebAuthn §34
├── send-push          → Web Push VAPID + AES-128-GCM (RFC 8291)
├── pattern-detect     → Claude behavioral patterns → behavioral_patterns
├── daily-brief        → Claude daily brief → daily_briefs (cached/day)
├── memory-manager     → CRUD personal_memory (JWT auth)
├── data-export        → PDPA export all data as JSON (JWT auth)
├── account-delete     → PDPA cascading delete + auth user
├── auth-rate-limit    → Rate limit check/increment/reset
└── account-recovery   → Passkey recovery via magic link
```

## ⚡ Vercel API Functions (13 ฟังก์ชัน)

```
api/
├── chat.ts          → Brain Gateway → Anthropic Claude
├── nova.ts          → Nova AI twin
├── coach.ts         → Decision coaching
├── intelligence.ts  → Astrovera Psychology
├── journal-sync.ts  → Offline journal + AI
├── personal-model.ts → AI feedback loop
├── push.ts          → Push subscription CRUD
├── profile.ts       → User profile
├── blueprint.ts     → Blueprint storage
├── share.ts         → Share links
├── autonomy-log.ts  → Autonomy scoring
└── stripe.ts        → Payments (§31)
```

---

## 🗄️ Supabase Tables (key)

| Table | หน้าที่ |
|-------|--------|
| `profiles` | User display info |
| `personal_profiles` | decision_style, strengths, blind_spots |
| `blueprints` | AI Twin blueprint (60–100%) |
| `chat_messages` | All chat history |
| `personal_memory` | small_win / important_moment / discovery / personal |
| `behavioral_patterns` | AI-detected patterns |
| `daily_briefs` | cached daily briefs (unique: user_id + date) |
| `auth_rate_limits` | Rate limiting (email/ip) |
| `push_subscriptions` | Web Push endpoints + VAPID keys |
| `subscriptions` | Stripe subscription tiers |
| `share_links` | Public share URLs |
| `analytics_events` | Usage analytics |
| `user_credentials` | WebAuthn passkey credentials |
| `passkey_challenges` | WebAuthn challenges (TTL) |
| `journal_queue` | Offline journal sync queue |

---

## 🆕 Components ใหม่ (2026-08-11)

| Component | Path | หน้าที่ |
|-----------|------|--------|
| TodaySection | `src/components/today/TodaySection.tsx` | AI Orchestrator เลือก sections ตามเวลา |
| ExplorePage | `src/pages/ExplorePage.tsx` | Hexagram + คำถาม + coming-soon cards |
| ActivitiesPage | `src/pages/ActivitiesPage.tsx` | 6 หมวด × activities, filter chips |
| MePage | `src/pages/MePage.tsx` | Profile, tier, links settings |

---

## ✅ Checklist ก่อนเริ่มงานต่อ

1. `git log --oneline -5` + `git status` — ดูว่าอยู่ตรงไหน
2. `npx tsc -b` — ต้อง EXIT 0 ก่อนทำงาน
3. ถ้ามีงานค้าง uncommitted → commit ก่อนเสมอ
4. อ่านไฟล์นี้เป็นหลัก ไม่ใช่ docs เก่า

---

## 🔑 กฎเหล็กที่ต้องจำ

```
CSS:    var(--...) เท่านั้น — ห้าม hardcode สี/ขนาด
userId: useAuth().session?.user?.id เท่านั้น
Guard:  if (!supabase) return ก่อนใช้ supabase client
Git:    git add ทุกไฟล์ใหม่ก่อน commit — untracked = Vercel fail
Build:  npx tsc -b ต้องผ่านก่อน push เสมอ
Scope:  แก้เฉพาะจุด อย่า refactor นอก scope
Mock:   ห้ามมี mock, placeholder, hardcode, dead code ใหม่
```

---

**Last Updated:** 2026-08-11 (Navigation complete — 5 tabs, TodaySection, 3 new pages)
**Updated by:** Claude (§5.1 + §5.2 complete)
