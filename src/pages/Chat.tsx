/**
 * Chat.tsx
 *
 * หน้า Chat หลัก (ใช้ ChatPage ใหม่)
 * รวม: HubSwitcher + EmotionSelector + ChatWindow + ChatInput
 */

import { ChatPage } from './ChatPage';

export default function Chat() {
  return <ChatPage />;
}